# Example Website

A simple static website starter — plain HTML, CSS, and JS, no build step required.

## Getting started

Open `index.html` directly in a browser, or serve it locally:

```bash
python3 -m http.server 8000
```

Then visit http://localhost:8000.
